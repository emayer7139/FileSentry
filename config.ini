# === FileSentry Config ===

# Space-separated list of directories to monitor
MONITOR_DIRS="/etc"

# Email address for sending alerts
ALERT_EMAIL="Your Email"

# Path to the log file (will be created if not present)
LOG_FILE="./filesentry.log"

# Path to the baseline file for storing file hash
BASELINE="baseline.db"
